import heapq
import time

def heuristic(a, b):
    """Υπολογισμός Manhattan απόστασης"""
    return abs(a[0] - b[0]) + abs(a[1] - b[1])

def a_star_search(grid, start, goal):
    """A* Algorithm - Υπολογισμός του shortest path και μέτρηση χρόνου"""
    rows, cols = len(grid), len(grid[0])
    open_set = []
    heapq.heappush(open_set, (0, start))
    came_from = {}
    g_score = {start: 0}
    
    start_time = time.time()  # Ξεκινά η χρονομέτρηση
    
    while open_set:
        _, current = heapq.heappop(open_set)

        if current == goal:
            end_time = time.time()  # Τέλος χρονομέτρησης
            return end_time - start_time  # Ταχύτητα εύρεσης (σε δευτερόλεπτα)

        for dx, dy in [(-1, 0), (1, 0), (0, -1), (0, 1)]:
            neighbor = (current[0] + dx, current[1] + dy)
            
            if 0 <= neighbor[0] < rows and 0 <= neighbor[1] < cols and grid[neighbor[0]][neighbor[1]] != 1:
                tentative_g_score = g_score[current] + 1
                
                if neighbor not in g_score or tentative_g_score < g_score[neighbor]:
                    g_score[neighbor] = tentative_g_score
                    priority = tentative_g_score + heuristic(neighbor, goal)
                    heapq.heappush(open_set, (priority, neighbor))
                    came_from[neighbor] = current

    return float('inf')  # Αν δεν βρεθεί μονοπάτι

# Παράδειγμα 10x10 grid
grid = [[0] * 10 for _ in range(10)]
grid[5][5] = 1  # Εμπόδιο

start = (0, 0)
goal = (9, 9)

execution_time = a_star_search(grid, start, goal)

print(f"Search Speed (Execution Time): {execution_time:.6f} seconds")

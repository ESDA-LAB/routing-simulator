import heapq
import time

def dijkstra_execution_time(graph, start, goal):
    start_time = time.perf_counter()  # Έναρξη μέτρησης χρόνου
    
    shortest_distance = {node: float('inf') for node in graph}
    shortest_distance[start] = 0
    pq = [(0, start)]
    
    while pq:
        current_distance, current_node = heapq.heappop(pq)
        if current_node == goal:
            break
        for neighbor, weight in graph[current_node]:
            distance = current_distance + weight
            if distance < shortest_distance[neighbor]:
                shortest_distance[neighbor] = distance
                heapq.heappush(pq, (distance, neighbor))
    
    end_time = time.perf_counter()  # Τέλος μέτρησης χρόνου
    execution_time = end_time - start_time
    
    print(f"Χρόνος εκτέλεσης: {execution_time:.6f} δευτερόλεπτα")
    return execution_time
